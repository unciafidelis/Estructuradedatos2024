import numpy as np
a = np.array([[1, 2, 3],[4, 5, 6],[9, 8, 7],[10, 5, 6]])
ordenado = np.array([])
print(a)
x = 0
i = 0
for l in a:
    for k in j:
        if k > x:
            ordenado = np.append(a, [k])
if (len(ordenado))